# data/

`topics.json` ở đây là **bộ luật mẫu**, để `bootstrap.mjs` chép sang repo note
của bạn. Các file `YYYY-MM.json` thật nằm trong repo note (private), không nằm
ở đây.

Xem `../CLAUDE.md` để biết định dạng.
